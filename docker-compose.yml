version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgresql://postgres:UmSCkPRPpogaPzMVFtfSbXEkiybRGnpY@yamabiko.proxy.rlwy.net:15089/railway
      NEXTAUTH_URL: http://localhost:3000
      NEXTAUTH_SECRET: replace-with-a-long-random-string
      JWT_SECRET: replace-with-another-long-random-string
    ports:
      - "3000:3000"
